name: CI - Weekly Data Cleanup

on:
  schedule:
    - cron: "0 4 * * 6"  # 04:00 UTC Saturday
  workflow_dispatch: {}

jobs:
  e2e:
    name: E2E - Weekly CMS Test Data Cleanup Run
    uses: e2e-allure-core.yml
    secrets: inherit
    with:
      cucumber_tags: "@weekly-data-cleanup"
      retention_days: 30
