default: --format pretty --tags "@wip"
chrome: BROWSER=chrome HEADLESS=true --format pretty
allure: >
  --format progress
  --format AllureCucumber::CucumberFormatter --out reports/allure-results
  --publish-quiet